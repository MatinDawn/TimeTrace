.list-card {
  margin-top: 24rpx;
}

.reminder-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
  padding: 24rpx 0;
  border-bottom: 1rpx solid var(--hairline);
}

.reminder-row:last-child {
  border-bottom: none;
}

.reminder-main {
  flex: 1;
}

.reminder-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--ink-800);
}

.reminder-meta {
  margin-top: 8rpx;
  font-size: 24rpx;
  color: var(--ink-500);
}
