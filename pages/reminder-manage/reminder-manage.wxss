.list-card {
  margin-top: 24rpx;
}

/* .reminder-row 共性 (display/align/justify/gap/padding/border-bottom + :last-child) 来自 .list-row */
.reminder-main {
  flex: 1;
}

.reminder-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--ink-800);
}

.reminder-meta {
  margin-top: 8rpx;
  font-size: 24rpx;
  color: var(--ink-500);
}
